[pytest]
# 瘦身后底座的验收/回归测试集中在根 tests/ 目录。
testpaths = tests
# 把仓库根加入 sys.path，使应用层 robot_agent/ 可被测试直接 import（无需单独打包）。
pythonpath = .
asyncio_mode = auto
addopts = -ra -q
filterwarnings =
    ignore:create_react_agent has been moved.*
